{
    "version": 2,
    "builds": [
      {
        "src": "app.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "/route/(.*)", "dest": "/route/$1" },
      { "src": "/(.*)", "dest": "/app.js" }
    ]
  }
  